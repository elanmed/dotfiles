The user has made local changes outside of this conversation.

Examine the diff (`git diff` and `git status`) to see what changed.

Treat the user's changes as authoritative — do not revert, argue with, or override them. Adapt your work to their changes.
