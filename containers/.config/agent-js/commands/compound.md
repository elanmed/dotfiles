Reflect on the current session and extract the durable lessons learned — things that would help future sessions avoid mistakes, follow the user's preferences, or work more effectively.

For each lesson, decide where it belongs:

- Applies across all projects (environment, tools, general workflow): update the global AGENTS.md at `/root/.config/.agent-js/context/AGENTS.md`
- Applies only to this project (conventions, structure, project-specific gotchas): update the local `./AGENTS.md` in the current directory
- Encodes a repeatable procedure or task: create or update a skill — global at `/root/.config/.agent-js/skills/<name>/SKILL.md`, or local at `./.agent-js/skills/<name>/SKILL.md`

Write lessons as concise, actionable rules — not a session log. Skip one-off details and anything already documented. Minimize diffs and make the changes directly.
