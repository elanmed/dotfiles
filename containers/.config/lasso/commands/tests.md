Check the working tree for diffs and review what changed since the last commit.

Add test coverage for any new features and update existing coverage to match any behavior changes.

Make sure the test suite, lint, and types all pass before finishing.
